name: "air_health_data_platform"
version: "1.0"
config-version: 2
profile: "air_health_data_platform"

model-paths: ["models"]
macro-paths: ["macros"]
test-paths: ["tests"]
target-path: "target"
clean-targets: ["target", "dbt_packages"]

models:
  air_health_data_platform:
    silver:
      +materialized: view
    gold:
      +materialized: table

  